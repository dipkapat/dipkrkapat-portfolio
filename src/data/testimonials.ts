import type { Testimonial } from "@/types";

// NOTE: Testimonials are intentionally empty. Per the content strategy, this
// section is never fabricated. Populate this array with genuine quotes from
// teammates, stakeholders, or collaborators to make the section render.
// Structure: { quote, person, position, company }
export const testimonials: Testimonial[] = [];
